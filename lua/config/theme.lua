require 'nordic'.setup {
  bold_keywords = true,
  cursorline = {
    bold = false,
    bold_number = false,
    theme = 'dark',
    blend = 0.85
  },
  bright_border = false,
  italic_comments = true,
  swap_backgrounds = true,
  telescope = {
    style = 'classic'
  }
}

require 'nordic'.load()
